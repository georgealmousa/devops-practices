
stages:
  - build

before_script:
  - echo "--------- STARTING WORK ------------"
  - $(./script.sh) 
variables:
  SCRIPT: $(./script.sh) 
package the maven project:
  stage: build
  script: 
  - mvn clean package 
  - sed -i "s/%%VERSION%%/$SCRIPT" ./devops_sample/pom.xml
  tags:
    - george
  allow_failure: false
  artifacts:
    paths:
      - target/*.jar 

